{
  "name": "the-slashinator",
  "version": "1.0.0",
  "description": "A cloud function to eliminate excessive costs in GCP",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "start": "functions-framework --target=slashinator --signature-type=cloudevent",
    "prestart": "npm run build",
    "gcp-build": "npm run build",
    "predeploy": "npm run lint && npm run test:run && npm run build",
    "deploy": "gcloud functions deploy the-slashinator --gen2 --runtime=nodejs24 --entry-point=slashinator --region=europe-west1 --source=. --trigger-topic=budget-alerts --service-account=${SERVICE_ACCOUNT:?SERVICE_ACCOUNT environment variable not set} --timeout=60s --memory=256MB --max-instances=10 --retry --project=${PROJECT_ID:?PROJECT_ID environment variable not set}",
    "prepare": "husky"
  },
  "author": "Jens Skott <jens.skott@thoughtgears.co.uk>",
  "license": "MIT",
  "dependencies": {
    "@google-cloud/billing": "^6.1.0",
    "@google-cloud/functions-framework": "^5.0.5",
    "p-retry": "^8.0.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "@typescript-eslint/eslint-plugin": "^8.70.0",
    "@typescript-eslint/parser": "^8.70.0",
    "@vitest/coverage-v8": "^5.0.1",
    "@vitest/ui": "^5.0.1",
    "eslint": "^10.10.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1",
    "typescript": "^6.0.3",
    "vitest": "^5.0.1"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "vitest related --run"
    ]
  }
}

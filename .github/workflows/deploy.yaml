name: Deploy Cloud Function

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Run tests
        run: npm run test:run

      - name: Login to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          project_id: ${{ secrets.PROJECT_ID }}
          workload_identity_provider: ${{ secrets.WIF_POOL_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy The Slashinator Cloud Function (Gen2)
        id: deploy
        shell: bash
        run: |
          gcloud functions deploy the-slashinator \
            --gen2 \
            --runtime=nodejs22 \
            --entry-point=slashinator \
            --region=europe-west1 \
            --source=. \
            --trigger-topic=${{ secrets.PUBSUB_TOPIC }} \
            --service-account=${{ secrets.APP_SERVICE_ACCOUNT }} \
            --timeout=60s \
            --memory=256MB \
            --max-instances=10 \
            --retry \
            --update-labels=env=production,team=thoughtgears,app=the-slashinator,owner=thoughtgears,gen=2 \
            --project=${{ secrets.PROJECT_ID }} \
            --quiet
